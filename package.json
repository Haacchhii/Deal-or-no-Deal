{
  "name": "unit-page",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "node scripts/validate-catalog.mjs && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test tests/*.test.mjs",
    "photos": "node scripts/prepare-photos.mjs"
  },
  "devDependencies": {
    "sharp": "0.35.4",
    "vite": "8.1.4"
  }
}
